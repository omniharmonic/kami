# Hermes-side only. Rendered by deploy-profile into ~/.hermes/profiles/<slug>/.env. Never commit a rendered copy.
# No chain key may ever live here — no wallet key, no seed phrase, no Safe signing or proposer key of any kind.
# The agent never signs (ADR-E05); the proposer key lives in the platform's KMS (architecture §10.2).
PLATFORM_MCP_TOKEN=
KAMI_ENTITY_SLUG=
